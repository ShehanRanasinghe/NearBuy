<?xml version="1.0" encoding="utf-8"?>
<!--
    item_dashboard_shop.xml
    Single shop row used in the "Nearby Shops" RecyclerView on the Dashboard.
    Flat 2-level hierarchy – one root LinearLayout + one inner vertical column.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:background="@drawable/bg_card_white"
    android:padding="14dp"
    android:layout_marginBottom="10dp"
    android:elevation="2dp"
    android:clickable="true"
    android:focusable="true">

    <!-- Icon circle – emoji TextView (random icon assigned by adapter) -->
    <TextView
        android:id="@+id/tvShopIcon"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:text="🏪"
        android:textSize="20sp"
        android:gravity="center"
        android:background="@drawable/bg_icon_circle_teal"
        android:contentDescription="Shop icon"/>

    <!-- Shop name + distance/status -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:layout_marginStart="12dp">

        <TextView
            android:id="@+id/tvShopName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="FreshMart Grocery"
            android:textColor="@color/text_dark_primary"
            android:textSize="14sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end"/>

        <TextView
            android:id="@+id/tvShopDistStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="📍 0.8 km  •  Open"
            android:textColor="@color/text_dark_secondary"
            android:textSize="12sp"
            android:layout_marginTop="2dp"/>

    </LinearLayout>

    <TextView
        android:id="@+id/tvShopDeals"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="5 deals"
        android:textColor="@color/nb_primary"
        android:textSize="11sp"
        android:textStyle="bold"
        android:background="@drawable/bg_distance_badge"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"
        android:paddingTop="3dp"
        android:paddingBottom="3dp"/>

</LinearLayout>

