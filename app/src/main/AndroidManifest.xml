<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NearBuy"
        tools:targetApi="31">

        <!-- Splash Screen - Launcher -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Screen -->
        <activity
            android:name=".Login"
            android:exported="false" />

        <!-- Home Screen -->
        <activity
            android:name=".HomeActivity"
            android:exported="false" />

        <!-- Search Screen -->
        <activity
            android:name=".SearchActivity"
            android:exported="false" />

        <activity
            android:name=".Settings"
            android:exported="false" />
        <activity
            android:name=".DashboardUI"
            android:exported="false" />
        <activity
            android:name=".Profile"
            android:exported="false" />
        <activity
            android:name=".Saved_Deals"
            android:exported="false" />
        <activity
            android:name=".deal_details"
            android:exported="false" />
        <activity
            android:name=".Store_Details"
            android:exported="false" />
        <activity
            android:name=".CategoriesActivity"
            android:exported="true"
            android:theme="@style/Theme.NearBuy.Categories"
            android:label="@string/categories_title" />
        <activity
            android:name=".NotificationsActivity"
            android:exported="true"
            android:theme="@style/Theme.NearBuy.Screen"
            android:label="@string/notif_screen_title" />
    </application>

</manifest>